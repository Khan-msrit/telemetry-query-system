name: Automatic Sync to Secondary Repo

on:
  push:
    branches:
      - main

jobs:
  sync:
    if: github.repository == 'Khan-msrit/telemetry-query-system'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout primary repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Push to secondary repo
        run: |
          git push --force https://x-access-token:${{ secrets.SECONDARY_REPO_TOKEN }}@github.com/lK-khan/telemetry-query-system.git main:main
